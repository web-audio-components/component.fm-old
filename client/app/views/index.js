(function () {
  app.views.Index = app.views.View.extend({

    name : 'index',

    className: 'index-view',
    
    initialize: function () {
      this.render();
    },

    events: {
    }
  });
})();
